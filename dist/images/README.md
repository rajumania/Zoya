# ZOYA POP DECORATIONS image library

Add approved project photography to this folder as it becomes available. The portfolio currently uses carefully selected temporary stock imagery so the layout can be reviewed without inventing project claims. Replace the image URLs in `src/App.jsx` with local paths such as `/images/project-living-room.webp` for the best production performance.
